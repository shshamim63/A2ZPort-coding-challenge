# A2ZPort-coding-challenge
To execute this code perform the following steps:

1. Clone the repository
2. execute ```cd A2ZPort-coding-challenge``` on terminal
3. execute ```node find-longest-word.js```